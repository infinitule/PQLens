# pqlens discovery fixture (authored by pqlens). Mixed TLS config.
server {
    ssl_protocols TLSv1.2 TLSv1.3;
    # Classical, quantum-vulnerable key exchange:
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256;
    ssl_ecdh_curve X25519:prime256v1;
    # PQC hybrid group (quantum-secure, hybrid-ready):
    ssl_conf_command Groups X25519MLKEM768:X25519;
}
